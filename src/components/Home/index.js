import React from "react";

const Home = ({description, year}) => (
  <div className="home">
    <blockquote>React Rubik is a small project with a big purpose. Create a set of useful components for future React Projects.</blockquote>
  </div>
);

export default Home;
